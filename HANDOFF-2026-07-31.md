# 交接文档 · 2026-07-31

> 写给**完全没有上下文**的新会话。读完这份就能接着干，不需要翻历史对话。
> 本轮主题：**观点回测（Insights Backtest）** 功能从 0 到 1 落地。

---

## 0. 项目速览（先读这段）

| 项       | 值                                                                                                                                  |
| -------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 仓库     | `fund-assistant`（`https://github.com/freakingryan/fund-assistant.git`，分支 `main`）                                               |
| 本地路径 | `/Users/murnysaul/WorkBuddy/2026-06-13-10-40-04/fund-assistant`                                                                     |
| 形态     | 纯前端 SPA + PWA，**零后端**，纯静态托管                                                                                            |
| 技术栈   | React 18 + TS + Vite / Tailwind v4 / shadcn 风格自建 UI / Zustand / Dexie(IndexedDB) / React Router v6 / Recharts / vite-plugin-pwa |
| 数据落地 | 全部在浏览器 IndexedDB，无服务端；密钥 BYOK 仅存本地                                                                                |

**运行命令（务必带 `NODE_OPTIONS=""` 前缀）**

```bash
cd /Users/murnysaul/WorkBuddy/2026-06-13-10-40-04/fund-assistant
NODE_OPTIONS="" npx vite          # dev（注意：用户习惯自己跑 dev server，AI 不要抢着起）
NODE_OPTIONS="" npx vite build    # 生产构建（真正的兜底校验）
NODE_OPTIONS="" npx eslint <files>
```

**硬性协作规则（用户定的，必须遵守）**

1. **未经明确授权不 push**。本次是用户明说「一并 push」才推的。
2. 每次 commit 同步维护 plan/todo 文档与 `README.md`，别让文档和代码脱节。
3. 清理文档用**归档不删除**：移动到 `archive/`（已有 `archive/planning/...`），禁止 `rm`。
4. 中国股市配色：**涨红跌绿**。货币符号 `¥`。

---

## 1. 任务目标

做一个「观点回测」闭环，解决用户的真实工作流：

> 读微信公众号文章 → 分享到腾讯 **ima** 并让 ima 结合市场分析、给出投资意见 → 把对话**保存到 ima 知识库** → **fund-assistant 自动取回这份投资意见，结构化、按日回看、并做回测验证准不准**。

拆成三个能力：

1. **录入**：从 ima 知识库同步 / 粘贴链接（经 ima 抓取）/ 直接粘文本 → 抽成结构化「投资方向」卡片，并快照当日市场。
2. **回看**：按日期（月历）浏览历史观点与方向卡片，可看 ima 完整分析全文。
3. **回测**：对历史方向做 **T+5** 命中率与收益统计（买中涨 / 卖中跌 = 命中）。

### 两个关键设计决策（别推翻，是反复确认过的）

- **双输入形态**：ima 返回的内容**可能已经是「结合市场 + 观点」的成品分析**。这种情况只做轻量方向抽取（`mode="ima-analyzed"`，**不重跑 AI**，省 token 也避免二次曲解）；只有原始文本才跑完整 `callAI`（`mode="raw-text"`）。两条链路都收敛到同一个 `directions[] + fullText` 结构。
- **ima 只做「取数源」**：ima OpenAPI **读不到会话历史**，只能读知识库。所以前提是用户在 ima 侧把对话「保存到知识库」。不做生成式分析、不碰 ima 笔记。

---

## 2. 已完成内容

### 2.1 交付状态：**P0–P6 全部完成，已 push**

| 阶段 | 内容                                                | 状态 |
| ---- | --------------------------------------------------- | ---- |
| P0   | 类型 / 设置 / DB 骨架                               | ✅   |
| P1   | 取数与分析层（ima / 市场快照 / 双形态抽取）         | ✅   |
| P2   | 录入编排视图                                        | ✅   |
| P3   | 时间线回看视图                                      | ✅   |
| P4   | 回测引擎 + 回测面板                                 | ✅   |
| P5   | 路由 / 侧边栏 / README / 计划文档                   | ✅   |
| P6   | 质量门（eslint + tsc + vite build + husky）+ commit | ✅   |

**相关提交**

- `f85f7cb` feat(insights): 观点回测 —— 17 文件 / +2473 行（主体功能）
- 紧随其后的 `fix(db)` —— 修复新用户主题映射未预置（见 §4.1）

### 2.2 文件地图（新会话按这张表定位）

**数据层**

| 文件                     | 作用                                                                                                                                                              |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/types/index.ts`     | 尾部新增 5 个接口：`InvestmentDirection` / `MarketSnapshot` / `Insight` / `ThemeMapping` / `ImaConfig`；`UserSettings` 增 `ima` 字段                              |
| `src/stores/db.ts`       | Dexie **v13** `insights`、**v14** `themeMappings`；`DEFAULT_THEME_MAPPINGS` 预置 9 类主题（半导体/新能源/医药/消费/军工/券商/红利/创业板/沪深300）→ 代表 ETF 代码 |
| `src/stores/settings.ts` | `defaultSettings.ima = { enabled, clientId, apiKey, kbId, kbFolderId, proxyUrl }`                                                                                 |

**服务层**

| 文件                              | 行数 | 作用                                                                                                                                                 |
| --------------------------------- | ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/services/ima.ts`             | 242  | ima OpenAPI 封装：`probeConnection` / `syncFromImaKb` / `fetchArticle` / `getMediaText` / `listKbMedia`；`ImaError`（auth/network/notfound/unknown） |
| `src/services/insightMarket.ts`   | 75   | `buildMarketSnapshot(date, mappings)` —— 宽基（沪深300/中证全指）+ 各主题 ETF 当日涨跌%，数据源 `fetchTencentKline`                                  |
| `src/services/insightAnalysis.ts` | 181  | `extractDirections`（Case A 启发式）/ `analyzeInsight`（Case B 走 `callAI`）/ `resolveMappedCodes`（主题关键词 → ETF 代码回填）                      |
| `src/services/insightBacktest.ts` | 282  | `runBacktest(filter)` —— T~T+5 收益、命中判定、按主题/按日聚合、累计收益曲线                                                                         |

**视图层**（均在 `src/components/insights/`）

| 文件                      | 行数 | 路由                                                                             |
| ------------------------- | ---- | -------------------------------------------------------------------------------- |
| `InsightInputView.tsx`    | 343  | `/insights` —— ima 同步 / 链接抓取 / 文本录入                                    |
| `InsightTimelineView.tsx` | 436  | `/insights/timeline` —— 月历 + 按日方向卡片 + 详情弹窗                           |
| `InsightBacktestView.tsx` | 465  | `/insights/backtest` —— 概览卡 + Recharts 累计收益曲线 + 按主题命中率 + 逐条明细 |

**接入点**：`src/constants/routes.ts`（`insights` / `insightsTimeline` / `insightsBacktest`）、`src/router.tsx`（3 条子路由挂在 AppLayout 下）、`src/components/layout/AppLayout.tsx:70`（侧边栏「观点回测」，图标 `Brain`）。

**文档**：`README.md`（功能说明 + 使用前提）、`plans/insights-backtest/{task_plan,findings,progress}.md`（决策与调研留档）。

### 2.3 核心口径（改代码前先理解）

**回测判定**（`insightBacktest.ts` 顶部注释即规范）

- 取观点日 T 到 T+5 的日 K（`KLINE_BACK = 400` 根，`HOLD_DAYS = 5`）。
- `buy` 且收益 > 0 → 命中；`sell` 且收益 < 0 → 命中。
- `hold` 计中性，**不计入**命中率。
- 取数缺口（无 T / T+5 数据，或 T+5 尚未发生）→ 标 `gap`，**排除出统计**，UI 用黄色标记。
- **按需计算、不写回原记录**（避免重复覆盖与数据陈旧）。

**ima API 契约**（踩过坑，见 §5）

- Base：`https://ima.qq.com`，路径前缀**必须** `/openapi/wiki/v1/`。
- 认证是**自定义 Header**，不是 Bearer：`ima-openapi-clientid` + `ima-openapi-apikey` + `Content-Type: application/json`。
- 成功判定 `code === 0`；`401` → 鉴权失败。
- 用到的端点：`get_addable_knowledge_base_list`（连通性探测）、`get_knowledge_list`（列举 KB 媒体）、`get_media_info`（笔记类直接带 `note_content`；网页类返回 `url_info.url` 需二次 fetch）、`import_urls`（`knowledge_base_id` + `folder_id` 均必填，URL 上限 1–10）。

---

## 3. 当前问题

> 按严重度排序。**#1 是阻塞级——功能目前用户实际用不起来。**

### 🔴 #1 设置页缺 ima 配置 UI（阻塞）

`settings.ima` 的类型和默认值都有了，但**全项目只有一处读**（`InsightInputView.tsx:66`），**没有任何地方能写**。用户无法填 `clientId` / `apiKey` / `kbId` / `kbFolderId` / `proxyUrl`，除非手动改 IndexedDB。
连带：`ima.ts` 里的 `probeConnection()` 已实现但**零调用方**，本意就是给设置页的「测试连接」按钮用的。

→ **这是新会话的第一优先级任务。**

### 🟠 #2 themeMappings 没有管理界面

只有 DB 预置的 9 类主题，用户不能增 / 删 / 改。主题映射直接决定 `mappedCodes`，进而决定回测能不能跑，属于核心可配置项。
参考现成模式：`src/components/settings/EtfMappingManager.tsx`（同类映射管理 UI 已有实现，照抄结构即可）。

### 🟠 #3 端到端未真机验证

目前**全部是静态校验**（eslint 0 error / scoped tsc 0 error / `vite build` 成功 / husky 全绿）。以下路径**一次都没真跑过**，因为需要用户本人的 ima 密钥：

- ima 真实 API 连通性与返回结构
- 浏览器直连 ima 的 CORS 表现（可能需要 `proxyUrl` 兜底）
- 从知识库同步 → 抽取 → 落库的完整链路
- 回测取数（腾讯 K 线）在真实观点数据上的表现

### 🟡 #4 Case A 抽取是启发式正则，不是强解析

`extractDirections` 依赖 ima 输出里 `- **主题**：理由` 这种子弹点格式。ima 输出格式一变，就会退化成「整体一张卡片」（有兜底，不会崩，但粒度丢失）。若实测发现格式经常不匹配，考虑改为对 Case A 也走一次轻量 AI 结构化。

### 🟡 #5 设计预期内的限制（不是 bug，但要跟用户讲清楚）

- **回测需 ≥5 个交易日**：当天刚录入的观点必然显示「取数缺口」。
- **ima 密钥约 1 个月有效期**，过期需重填。
- **前提条件**：用户必须先在 ima 侧把对话保存进知识库，否则同步取不到东西。

---

## 4. 下一步计划

### 4.1 本次已顺手修掉的（已包含在交接提交里）

**Dexie 全新库不预置主题映射** —— `db.ts` 的 v14 `.upgrade()` 回调**只在从低版本升级时触发**。全新库（新浏览器 / 清空存储）会直接按最新版本建表并**跳过所有 upgrade**，导致新用户 `themeMappings` 为空 → `mappedCodes` 恒为空 → **整条回测链路空转**。
已补 `this.on("populate", ...)` 为新库预置，代码里留了警示注释。

### 4.2 待办（建议顺序）

| 优先级 | 任务                      | 说明                                                                                                                                                                                       |
| ------ | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| P1     | **设置页加 ima 配置区**   | 5 个字段 + `enabled` 开关 + 「测试连接」按钮接 `probeConnection()`。密钥输入用 password 型，与现有 AI key 同等待遇（仅存本地 IndexedDB）。落点：`src/components/settings/SettingsPage.tsx` |
| P2     | **themeMappings 管理 UI** | 增删改主题 → 代表标的。照抄 `EtfMappingManager.tsx`                                                                                                                                        |
| P3     | **真机联调**              | 让用户填入真实 ima key，跑通同步 → 抽取 → 回测全链路，记录实际返回结构，回填 `plans/insights-backtest/findings.md`                                                                         |
| P4     | **CORS 兜底验证**         | 若直连被拦，部署 Cloudflare Worker 纯转发（契约：POST `{url, method, headers, body}`，只补 CORS、**不持密钥**），填入 `proxyUrl`                                                           |
| P5     | 按实测结果打磨            | Case A 抽取准确率、回测口径微调（是否支持 T+10/T+20 可选窗口）                                                                                                                             |

---

## 5. 踩过的坑（血泪，务必先读再动手）

### 5.1 工具链 / 校验（最容易浪费时间的）

- **`husky` 的 `tsc --noEmit` 实为 no-op**。它跑的是仓库根 `tsconfig.json`——solution 风格（`files: []` + references），非 build 模式 → 只编译空 files 集，**恒 0 error**。真正兜底的是 `eslint`（仅暂存文件）+ `vite build`。
- **全量 `tsc -p tsconfig.app.json` 有 40+ 预存 error**（TS 6.0.3 + `erasableSyntaxOnly` 禁 enum、`verbatimModuleSyntax` 下的类型问题等），是**历史遗留工具链问题，不是你改出来的**。
  → 正确姿势：`tsc --noEmit -p tsconfig.app.json 2>&1 | grep -E "<你改的文件>"`，只看自己那几个文件，别被洪水淹没。
- **`tsc` 退出码陷阱**：别写 `tsc ... | head` 然后看 `$?`——那是 `head` 的退出码（恒 0），会掩盖真实失败。要么直接 `tsc --noEmit; echo "$?"`，要么用 grep 看内容。
- **所有 node/npx 命令必须带 `NODE_OPTIONS=""` 前缀**；`npm install` 用 `--force` 绕 peer dep 冲突。
- Bash 工具每条命令 cwd 会重置，需要显式 `cd`。

### 5.2 本轮新踩的

- **`formatDateOnly(ts: number)` 收的是时间戳数字，不是 `Date` 对象**。传 `new Date()` 会报类型错。本轮在 `InsightInputView` / `InsightTimelineView` 各踩一次。
- **Dexie `.upgrade()` 不覆盖全新库**（详见 §4.1）。凡是「首启预置数据」都必须同时挂 `on("populate")`，否则只有老用户有数据、新用户没有——这种 bug 在本地开发时几乎发现不了，因为你的库早就是升级上来的。
- **`interface` 体内不能写 `export type` / `export interface`**，类型声明必须放模块顶层，否则 husky 直接拦。
- **ima API 前缀**：必须 `/openapi/wiki/v1/`，用旧的 `/wiki/v1/` 或 `/api/v1/` 会 401；认证是自定义 header 不是 Bearer——这两点各坑过一次。

### 5.3 项目既有的重要约束（历史积累）

- **`stock-sdk` 是第三方只读依赖**（`github.com/chengzuopeng/stock-sdk`），**不能改也不能 push**。本项目只是它的 npm 消费方，唯一可维护的仓库是 `fund-assistant`。任何「去上游补一下」的念头都不成立（无权限）。
- **CORS 现实**：本项目是浏览器 SPA，直连 `*.eastmoney.com` 等会被 CORS 拦。Cloudflare Worker 是**CORS 反代**（服务端代抓回传），跟「东财通不通」是两回事——网络能通，CORS 照样拦。
- 不手写东财 / 腾讯的 fetch + 解析，优先用 `stock-sdk` / `stock-api` 已覆盖的能力。（例外：本轮 `insightMarket.ts` 复用了项目既有的 `fetchTencentKline`，那是早已存在的适配器，不是新写解析。）
- **`stock-sdk` 门面方法名是「简名」**：如 `sdk.calendar` 暴露 `marketStatus` 而非 `getMarketStatus`；市场参数枚举是 `'A' | 'HK' | 'US'`（A 股用 `'A'`，**不是 `'CN'`**）。
- **`sdk.report` 在已发布的 v2.4.0 里是 `undefined`**（研报 service 未发版），调用会崩页面，需要守卫。
- **`vite-plugin-pwa` 用 `injectManifest` 策略时**，`maximumFileSizeToCacheInBytes` / `globPatterns` 必须写在 **`injectManifest` 键**下，写在 `workbox` 键下完全不生效（默认 2MiB 上限会让 2.3MB 的主包构建失败）。且该版本 schema **拒收** `cleanupOutdatedCaches` / `navigateFallback` / `runtimeCaching`（generateSW-only）。

---

## 6. 当前仓库状态

- 分支 `main`，工作区干净，**已与 `origin/main` 同步**（本次已 push）。
- 最近提交：
  - `HANDOFF` + `fix(db)`（本次）
  - `f85f7cb` feat(insights): 观点回测主体
  - `c3736f6` chore(skills): frontend-quality-workflow 入库
  - `77ea164` feat(Phase17): Web Push 后台定时扫描
- 构建体积：主包约 **2.33 MB**（< PWA 预缓存 6 MiB 上限，无 precache 报错）。
- `PENDING_PLAN.md` 里「观点回测」条目已移除（功能已落地，决策上下文留在 `plans/insights-backtest/` 与 `README.md`）。

---

## 7. 新会话的第一步建议

```bash
cd /Users/murnysaul/WorkBuddy/2026-06-13-10-40-04/fund-assistant
git log --oneline -5
cat plans/insights-backtest/task_plan.md      # 看完整实施记录
cat plans/insights-backtest/findings.md       # 看 ima API 调研结论
```

然后直接开 §4.2 的 **P1：给设置页加 ima 配置区**——这是当前唯一挡住用户真正用起来的东西。
